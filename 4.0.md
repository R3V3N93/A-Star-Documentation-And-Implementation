# A* 알고리즘
---
지금까지 소개한 알고리즘들은 시작 지점에서 목적 지점까지의 최단 거리를 찾는 데에 매우 탁월한 성능을 보인다.  하지만 근본적으로 그래프의 모든 지점을 고리를 통해 탐색하고, 이를 토대로 최단 거리를 산출하기 때문에 그래프의 규모에 비례해 매우 비효율적이게 된다는 단점이 있다. 그렇다면 이를 해결할 수 있을까? 물론이다. 이를 알기 위해선 '휴리스틱 탐색(Heuristic Search)'를 먼저 알고 넘어가자.

휴리스틱 탐색이란, 영단어 heuristic에서 드러나듯, 알고리즘이 경험적으로, 즉 가능성이 높은 방향으로 나아가게 하는 방식으로, 쉽게 말해서 알고리즘이 '찍어 맞추는' 방식이다. 이러한 설명은 휴리스틱 탐색에 대해 부정적으로 생각할 여지를 제공할 수도 있지만 전혀 나쁜 방법이 아님을 염두해 두어라. 휴리스틱 탐색은 단일 개념이 아닌 단순히 상술한대로 가능성이 높은 방향으로 나아가게 하는 모든 방법을 뜻하기에 사용하는 곳에 따라, 그리고 사용하는 목적에 따라 달라질 수도 있다. 그러나 이 가이드에서는 아래와 같은 단순한 방법을 사용하겠다:

```csharp
double Heuristic(Vector2 a, Vector2 b)
{
	// 맨해튼 거리!
	return abs(a.x - b.x) + abs(a.y - b.y)
}
```

return 문의 식을 보면 알겠지만, 해당 휴리스틱 탐색 함수는 '맨해튼 거리(Manhattan Distance)'를 차용한다. 맨해튼 거리는 대각선 이동을 고려하지 않지 않나? 라고 생각할 수도 있다. 해당 가이드는 대각선이 없는 그리드 형식의 그래프만을 다룸을 항상 유념해두자. 당신이 이를 실제로 구현할 때 ,<u>다른 그래프 형식을 사용하고자 한다면 다른 휴리스틱 탐색 함수를 고려해야할 것이다</u>. 

아무튼, 우리가 휴리스틱 탐색을 다익스트라 알고리즘에 적용하는 목적은 <u>'현재 노드가 목적지 노드와 얼마나 가까운가?'</u>를 구하는 것으로 이해하자. 

이 휴리스틱 탐색을 다익스트라 알고리즘에 사용했던 PriorityQueue의 priority를 결정하는데 응용한다면 어떨까? 알고리즘은 목적지 노드와의 거리에 따른 그래프 탐색 루트의 우선 순위를 결정하게 될 것이다. 이렇게 휴리스틱 탐색과 다익스트라 알고리즘을 결합한 알고리즘이 바로 A* 알고리즘이다.

의사 코드는 다음과 같다:
```C#
PriorityQueue<Node> frontier = new PriorityQueue(); 
frontier.Add(START_NODE, 0); 


Dictionary<Node, int> cost_so_far = new Dictionary<Node, int>(); 
cost_so_far[START_NODE] = 0;
Dictionary<Node, Node> came_from = new Dictionary<Node, Node>(); 
came_from[START_NODE] = null;

while (frontier.Size() > 0)
{
	Node currentNode = frontier.Get(); 
	
	if(currentNode == DESTINATION_NODE) 
		break; 
		
	for (Node neighbor in currentNode.Neighbors()) 
	{
		int tempCost = cost_so_far[currentNode] + currentNode.Cost(neighbor);
		if (cost_so_far.Find(neighbor) == false || tempCost < cost_so_far[neighbor]) 
		{
			cost_so_far[neighbor] = tempCost;

			// 다익스트라 알고리즘에 이 한줄만 추가하면 된다.
			// 이제 이웃과 목적지 노드의 거리가 멀다면, PriorityQueue 상에서
			// 뒤로 밀려나게 될 것이다.
			int priority = tempCost + Heuristic(DESTINATION_NODE.pos, neighbor.pos);
			
			frontier.Add(neighbor, priority); 
			came_from[neighbor] = currentNode; 
		}	
	}		
}
```